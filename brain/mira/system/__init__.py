"""System control surface.

Stage 3 delivered tool-use via the agent loop: declarations live in
`brain/mira/agent/tools.py`, execution lives in the Mac client. The brain
only mediates round-trips and runs the `remember` tool itself.

Future server-side tools (web search, file indexing, etc.) will live here.
"""
